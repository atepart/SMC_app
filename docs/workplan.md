# Рабочий план

Здесь находятся только текущие и ближайшие согласованные задачи. Правила ведения описаны в [`AGENTS.md`](../AGENTS.md).

## В работе

- Нет задач.

## Запланировано

- `WP-001` — восстановить воспроизводимый цикл разработки через `uv` (связано с `BUG-001`).
  - Результат: проект корректно распознаётся `uv`, зависимости разработки описаны декларативно, стандартный pre-commit check запускается командами из `AGENTS.md`.
  - Критерии готовности:
    - `rtk uv sync` завершается успешно;
    - все команды из минимального pre-commit check реально запускаются;
    - приложение стартует командой `rtk uv run python -m aocapp`;
    - результат запуска GUI подтверждён ручной проверкой и скриншотом.

## Заблокировано

- Нет задач.
